Console.WriteLine("Hello, World!");

// A venue is the building itself.
// A theatre is each screening room inside the venue.
// Each theatre could be regular, IMAX, DBox (4D theatre).
// Each theatre has a boolean value of whether it has full reclining seats or not. All 3 theatre types can have reclining seats.
// Use TXT/MD files for storing the data since we can't use json parsing functions unless you want to reimplement them yourself.
// Use LinkedLists maybe since we can use LINQ now
// could use a circular queue for showtimes